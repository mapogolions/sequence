{
  "name": "sequence",
  "version": "1.0.0",
  "description": "Purely functional iterators",
  "main": "sequence.js",
  "scripts": {
    "test": "jest",
    "format": "prettier *.js src/*.js test/*.js --write",
    "checkstyle": "prettier *.js src/*.js test/*.js --check",
    "test:coveralls": "jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "keywords": [],
  "author": "mapogolions",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.4",
    "jest": "^24.8.0",
    "prettier": "1.18.2"
  }
}
